#ifndef TREAT_TYPES_CHAR_IH
#define TREAT_TYPES_CHAR_IH

#include "char.h"
#include "../../treat.h"
#include "container_of.h"

// Functions exposed through module:
TChar *Char_new(char character);
void Char_destroy(TChar *character);

// Internal functions:
void *Char_ctor(TraitObject *self, va_list *args);
void *Char_dtor(TraitObject *self);

#endif //TREAT_TYPES_CHAR_IH
